<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Izin yang diperlukan untuk akses internet -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Izin yang diperlukan untuk akses lokasi -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Jurnalify">
        <!-- Activity untuk menangani permintaan izin lokasi -->
        <activity
            android:name=".LocationPermissionActivity"
            android:exported="true"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar"
            android:windowSoftInputMode="adjustResize" />

        <!-- Activity tampilan bendera setelah izin lokasi -->
        <activity
            android:name=".LocationFlagActivity"
            android:exported="false"
            android:theme="@style/Theme.Jurnalify" />

        <!-- Activity utama -->
        <activity
            android:name=".MainActivity"
            android:exported="false"
            android:theme="@style/Theme.Jurnalify" />

        <!-- Activity tambah jurnal -->
        <activity
            android:name=".AddJournalActivity"
            android:exported="false"
            android:theme="@style/Theme.Jurnalify" />

        <!-- Activity detail jurnal -->
        <activity
            android:name=".JournalDetailActivity"
            android:exported="false"
            android:theme="@style/Theme.Jurnalify" />

        <!-- Activity edit jurnal -->
        <activity
            android:name=".JournalEditActivity"
            android:exported="false"
            android:theme="@style/Theme.Jurnalify" />

        <!-- Splash Screen -->
        <activity
            android:name=".SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.Jurnalify.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>